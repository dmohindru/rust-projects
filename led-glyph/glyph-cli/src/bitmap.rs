use crate::cli::AnimationMode;
pub fn generate_char_bitmap(grid_size: u8, character: char) -> Vec<u8> {
    todo!()
}

pub fn generate_string_bitmap(grid_size: u8, string: String, mode: AnimationMode) -> Vec<u8> {
    todo!()
}
